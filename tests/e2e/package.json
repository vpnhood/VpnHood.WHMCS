{
  "name": "vpnhood-whmcs-e2e",
  "private": true,
  "description": "Browser tests for the hub WHMCS store flows (cart warning, reseller CSV) against the dev install only.",
  "type": "module",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
